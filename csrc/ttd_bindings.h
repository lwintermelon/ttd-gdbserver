// ═══════════════════════════════════════════════════════════════════
// TTD SDK Bindgen Manifest
//
// Includes TTD SDK headers for auto-generating Rust type bindings
// via bindgen in C++ mode. This file is NOT a C API — it's a bindgen
// target that selects which upstream types to expose.
//
// C++ wrapper accesses these types via the same SDK includes directly.
// ═══════════════════════════════════════════════════════════════════
#include <TTD/IReplayEngine.h>
